@{
  Layout = "_Layout";
}

@model PurrBnB.Models.Reservation
<h2>Enter new reservaton</h2>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.ReservationId)
  @Html.TextBoxFor(model => model.ReservationId)
  
  @Html.LabelFor(model => model.TotalCost)
  @Html.TextBoxFor(model => model.TotalCost)

  @Html.Label("Add Reservation")
  @Html.DropDownList("ReservationId", "Select Dwelling")

  <input type="submit" value="Save" />
}
