@{
  Layout = "_Layout";
}

@model PurrBnB.Models.Pet

<div class="row">
	<div class="col-md-12">
		
    <div class="card">
      <div class="card-header">
        <h1 id="title" style="font-size:4vw">@Html.DisplayFor(model => model.Name) Details </h1>
      </div>
      <div class="card-body">
        <table class="table">
          <thead>
          <tr>
            <th>PetId</th>
            <th>Name</th>
            <th>Age</th>
            <th>Color</th>
            <th>Interests</th>
            <th>Favorite Toys</th>
            <th>Schedule</th>
            <th>Personality</th>
            <th>Species</th>
          </tr>
          </thead>
          <tbody>
          <tr>
            <td>@Html.DisplayFor(model => model.PetId)</td>
            <td>@Html.DisplayFor(model => model.Name)</td>
            <td>@Html.DisplayFor(model => model.Age)</td>
            <td>@Html.DisplayFor(model => model.Color)</td>
            <td>@Html.DisplayFor(model => model.Interests)</td>
            <td>@Html.DisplayFor(model => model.FavoriteToys)</td>
            <td>@Html.DisplayFor(model => model.Schedule)</td>
            <td>@Html.DisplayFor(model => model.Personality)</td>
            <td>@Html.DisplayFor(model => model.Species)</td>
          </tr>
          </tbody>
        </table>
      </div>
    </div>
    
	</div>
</div>

@* @if (@Model.JoinEntities.Count == 0) *@
  {
  <h3>This pet does not currently have a dwelling</h3>
  }
  else
  {
    <table class="table">
      <thead>
        <tr>
          <th></th>
          <th>Dwelling</th>
          <th></th>
          <th></th>
        </tr>
      </thead>
    <tbody>
    @* @foreach (var join in Model.JoinEntities) *@
      {
      <tr>
        <td></td>
        <td>@* <h2>@join.Dwelling.Type</h2> *@</td>
        <td></td>
        <td></td>
        <td>
          @* @using (Html.BeginForm("DeleteDwelling", "Pets")) *@
          @* { *@
          @*   $1$ @Html.Hidden("joinId", @join.DwellingPetId) #1# *@
          @*   <input type="submit" value="Remove Dwelling" id="button" class="btn btn-outline my-sm-0" /> *@
          @* } *@
        </td>
        <td></td>
        <td></td>
        <td></td>
        <td></td>
      </tr>
      }
    </tbody>
  </table>
  }
@* <button class="btn btn-outline">@Html.ActionLink("Add a Dwelling", "AddDwelling", new { id = Model.PetId })</button> *@
<button class="btn btn-outline">@Html.ActionLink("Back to listings", "Index")</button>